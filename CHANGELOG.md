# CHANGELOG

Si473X Radio Ver6 のリリース履歴です。  
バージョン番号は `vメジャー.マイナー.パッチ` 形式です。

---

## [v6.0.5] - 2026-07-25

### 修正
- LSB/USBチューニング時、`rx.setSSBBfo()` へ渡すBFO値の符号が実際の受信周波数の変化方向と逆になっており、以下の不具合が発生していた問題を修正
  - kHz境界を跨ぐとビートが途切れる／無音になる
  - 周波数を上げていく方向と下げていく方向でゼロビート位置がずれる
  - 同調してきた経路（アプローチ方向）によって、同じダイヤル表示でも実際の受信周波数が異なる
  - BFO送信直前に符号を反転することで、ソフト内部で計算しているダイヤル周波数と実際にチップへ命令している内容を一致させた（`startSSB()`／`startCW()`／WebリモコンFBコマンド／エンコーダー同調処理の4箇所に統一して適用）
- 上記に伴い、kHz境界を跨いだ（リチューンした）直後にAGCを再適用するよう変更。リチューン直後にノイズ・ミュートに近い状態が残ることがあった問題への対策
- LSB/USBのBFOキャリー幅を ±1000Hz → ±15000Hz に拡大し、通常のSSB微同調（50〜500Hzステップ）で1kHzごとに頻発していたリチューン音（"バサッ"というノイズ）の発生頻度を約1/15に低減
  - CWモードはサイドトーン基準（既定800Hz）を維持するため、キャリー幅は従来通り±1000Hzのまま変更していない
- ファイル　firmware/RP2040/SI473xRadio_Ver6.ino.uf2
---

## [v6.0.4] - 2026-07-23

### 追加
- Web リモコンに CW 自動判読パネル（📟 CW自動判読）を追加
  - Goertzel アルゴリズムによる単一トーン検出。既存の Audio スペアナ用オーディオグラフに非破壊で相乗りする形で実装
  - ノイズフロア／信号ピークを個別に追従する適応しきい値＋ヒステリシスで ON/OFF 判定
  - マーク長から dit/dah を判定し、ユニット長（WPM）を信号から自動追従
  - トーン周波数を中心に ±200Hz を自動スキャンし、ゼロインのズレを許容
  - 帯域内ミニスペクトラムを表示し、混信時にどの信号をデコード中か視覚化。クリックで対象信号を選択可能
  - パネル表示順・表示/非表示機能に登録

### 修正
- スペクトラムパネル（`data-panel="spectrum"`）の閉じ `</div>` が不足しており、ウォーターフォール以降のパネル（CW自動判読・プリセット等）が DOM 上でスペクトラムパネルにネストされていた不具合を修正。パネル表示/非表示機能でスペクトラムパネルを非表示にすると、道連れで他のパネルまで消えてしまっていた
- CW自動判読の判定処理を `requestAnimationFrame`（画面リフレッシュレート依存、≒16.7ms 分解能）から `setTimeout` 5ms 間隔の専用ループに分離し、描画処理と切り離すことでタイミング量子化誤差による誤読を低減

---

## [v6.0.3] - 2026-07-22

### 修正
- 国内放送局リスト（japan_stations.json）の周波数・コールサインを総務省告示ベースの呼出符号表等の一次資料と全件照合し、誤りを修正
  - NHK：AM局は運用休止のため削除、FM局42局のコールサインを修正
  - 民放AM局・FM局：周波数・コールサインの取り違え（他局・県またぎでの入れ替わり）を修正
  - コミュニティFM局：一次資料と突合できた40局の周波数・コールサインを修正
  - 実在しない局・廃局・誤分類エントリ、完全重複エントリを削除

### 注意
- コミュニティFM局のうち約70局は表記ゆれにより一次資料と機械的に突合できず、未確認のまま残存（今後の課題）

### ドキュメント
- 操作説明書（Si473x_Radio_Manual）「よくある質問とトラブルシューティング」に、PCでWiFi経由アクセス時にremote.htmlが古いキャッシュのまま表示される事象への対処を追記
  - 簡易対処：対象ページのみハード再読み込み（Ctrl+Shift+R）
  - 根本対処：Chromeの閲覧履歴データ削除（キャッシュされた画像とファイル）

---

## [v6.0.2] - 2026-07-21

### 修正
- EiBi リストのダウンロードが LittleFS 容量不足や通信瞬断で途中終了した際、壊れたキャッシュがそのまま正常なものとして使われてしまう不具合を修正（書き込みバイト数の検証・一時ファイル経由での安全な差し替えに変更）
- EiBi 自動更新が、パーティション容量の都合で以後ずっと失敗し続けてしまう問題に対応。空き容量が不足している場合のみ旧キャッシュを削除してから再取得するようにし、それでも失敗した場合は自動リトライ（最大3回）するように変更
- EiBi 取得先 URL が旧シーズン（`sked-b25.csv`）に固定されたままで、季節が変わっても自動更新されていなかった問題を修正。現在日時（UTC）から A/B シーズンを自動判定し、URL を自動的に切り替えるように変更
- Web リモコン「EiBi インポート」画面で、一覧の描画が先頭1000件（概ね1780KHz付近）で止まっていた問題を修正。ページネーション（前の1000件 / 次の1000件）に対応
- 上記に伴い、2ページ目以降を表示した状態で「プリセットに追加」「今すぐチューニング」を行うと、チェックした行と異なる局が反映されてしまう不具合を修正

### 追加
- 破損・中途半端になった EiBi キャッシュを手動で強制クリアできる `/eibi_clear` エンドポイントを追加

---

## [v6.0.1] - 2026-07-16

### 修正
- EiBi リスト自動取得のバグを修正

---

## [v6.0.0] - 2026-07-18

### 正式リリース（関ハム2026 頒布版）

#### ファームウェア（RP2040）
- LCD 表示不具合の解消
- RP2040 フリーズ問題の解決
- Audio スペアナの追従性向上・スケール視認性向上

#### Web リモコン
- 表示不良の改善
- Audio スペアナの追従性向上・スケール視認性向上

#### Android アプリ
- Audio スペアナが表示されない問題を修正

#### ドキュメント
- 操作説明書（Si473x_Radio_Manual.pdf）追加

---

## [v6.0.0-rc.3] - 2026-07-05

### 修正
- LCD 表示不具合の解消
- RP2040 フリーズ問題の解決
- Web リモコンの表示不良改善
- Android アプリ：Audio スペアナが表示されない問題を修正

### 改善
- Audio スペアナの追従性を向上
- Audio スペアナのスケール視認性を向上

---

## [v6.0.0-rc.2] - 2026-06-20

### 修正
- タイムアウト時に `_bandChangePendingBand = null` をセット
- 書き換え禁止が解除されず `bandSel` が古いまま残る問題を解消

---

## [v6.0.0-rc.1] - 2026-06-15

### 初回テストリリース

#### ファームウェア
- RP2040 ファームウェア 初版
- ESP32-C3 ファームウェア 初版

#### Web リモコン
- remote.html 初版
- AM / FM / SW / AIR バンド対応
- Web ブラウザからのリモート操作

#### アプリ
- Windows コントロールアプリ 初版
- Android コントロールアプリ 初版

#### ドキュメント
- 操作説明書 初版

---

## リリースノートの見方

| 種別 | 内容 |
|------|------|
| `追加` | 新機能の追加 |
| `変更` | 既存機能の変更・改善 |
| `修正` | バグ修正 |
| `削除` | 機能の削除 |
| `注意` | アップデート時の注意事項 |

---

## アップデート手順

### ファームウェア更新

**RP2040**
1. BAND+ と BAND− を両方押したまま POWER ボタンで電源 ON
2. PC に「RPI-RP2」ドライブとして認識されたらボタンを離す
3. `.uf2` ファイルを RPI-RP2 ドライブにコピー

**ESP32-C3**
1. ブラウザで `http://[IPアドレス]/update` にアクセス
2. `.merged.bin` をアップロード

### Web リモコン更新

1. ブラウザで `http://[IPアドレス]/upload` にアクセス
2. `remote.html.gz` をアップロード
